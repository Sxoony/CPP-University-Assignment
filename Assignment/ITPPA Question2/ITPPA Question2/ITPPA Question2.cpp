#include <iostream>
#include <math.h>
#include <string>
#include <fstream>
using namespace std;
int main() {
	//1)select items, 2)specify quantities, 3)calculate total cost, 4)apply discount of 5% for orders over R200
	// , 5)write to final bill
	//prompt user for name, selected items from menu,and enter quantities, after calculating final cost output
	//final bill and save details to text file.
	string name;
	int i, numItems, cost, totalcost;
	

}